import React from 'react'

const page = () => {
  return (
    <div className='flex text-[var(--primary-blue)] flex-col justify-center items-center w-full h-full p-5 border-[var(--primary-blue)] border-[0.25px] rounded-md bg-[var(--primary-white)]'>
      This is Overview page under construction
    </div>
  )
}

export default page
